# خطة Codex 5.3 للفحص البصري والتشغيلي الكامل

## قرار الجاهزية الحالي

الحالة الحالية ليست "جاهزة تسليم نهائي" بعد.

السبب: الفحوص الآلية الأساسية نجحت، لكن الفحص البصري داخل المتصفح كشف مؤشرات Runtime يجب التعامل معها كجزء من تسليم حقيقي، منها:

- تحذير `Failed to hydrate non-critical session data`.
- خطأ Service Worker عند تحديث `sw.js`.
- تحذير `App bootstrap fallback active` بسبب `Cannot read properties of null (reading 'map')`.
- ضرورة عزل جلسة المدير عن جلسات الطالب/المعلم/ولي الأمر للتأكد أن الحماية لا تعتمد على واجهة فقط.

القاعدة: لا يتم إعلان `READY FOR DELIVERY` إلا بعد فحص بصري فعلي متعدد الأدوار، وليس فقط بعد نجاح `smoke`.

## قاعدة العمل المستمرة

عندما يقول صاحب المشروع "اكمل":

- يبدأ Codex دفعة جديدة تلقائيا إذا كانت الدفعة السابقة مغلقة.
- لا يكتفي بالرد النصي.
- ينفذ فحصا فعليا في المتصفح الداخلي إن كان المطلوب واجهة أو تجربة مستخدم.
- يوثق ما تم اختباره وما فشل وما أصلح.
- لا يطلب مفاتيح جديدة إلا إذا فشل وصول حقيقي وتم تسجيل رسالة الخطأ الفنية.

## هدف الفحص

تحويل Codex إلى فاحص بصري وتجريبي حقيقي للموقع:

- يتصفح كزائر.
- يسجل دخول وخروج بالحسابات المتاحة.
- يجرب المدير والطالب والمعلم والمشرف وولي الأمر.
- يفحص console وnetwork وسلوك الأزرار والنماذج والصفحات بعد التحديث والرجوع.
- يصنف المشاكل حسب الخطورة ويصلح Critical/High قبل أي إعلان جاهزية.

## الحسابات المعتمدة للفحص

تستخدم هذه الحسابات في الفحص البصري والآلي عند الحاجة:

- المدير: الجلسة الحالية في المتصفح الداخلي أو التوكن التشغيلي المؤقت عند فحص API.
- المعلم: `teacher.quant@almeaa.local` / `Teacher@123`
- الطالب: `student.a@almeaa.local` / `Student@123`
- ولي الأمر: `parent.a@almeaa.local` / `Parent@123`
- المشرف: `supervisor.group@almeaa.local` / `Supervisor@123`

لا يتم طباعة التوكنات أو الأسرار في التقارير.

## المرحلة 1: تثبيت خط الأساس

1. قراءة الحالة:
   - `CODEX_HANDOFF.md`
   - `PROJECT_STATUS.md`
   - `docs/NEXT_SESSION_HANDOVER_AR.md`
   - `docs/SPARK_BATCH_LEDGER_AR.md`

2. تثبيت مرجع الإنتاج:
   - آخر commit على `main`.
   - commit الموجود على Vercel production.
   - حالة API وMongo وRender.

3. تشغيل البوابة الأساسية:
   - `npm run typecheck`
   - `npm run build`
   - `npm run server:check`
   - `npm run server:build`
   - `npm run smoke:health-readiness`
   - `npm run smoke:frontend:strict`
   - `npm run smoke:operational`

## المرحلة 2: فحص المتصفح الداخلي

يجب استخدام المتصفح الداخلي كأداة أساسية للفحص، وليس كإثبات شكلي.

لكل صفحة يتم تسجيل:

- الرابط.
- الدور المستخدم.
- النص أو الحالة الظاهرة.
- أخطاء console.
- أخطاء network 4xx/5xx.
- سلوك refresh.
- سلوك back/forward.
- هل يوجد blank screen أو infinite loading.

## المرحلة 3: رحلة الزائر

يتم اختبار:

- الصفحة الرئيسية.
- صفحة العضويات `/pricing`.
- صفحات المسارات العامة.
- المدونة.
- السلة.
- محاولة فتح مسارات محمية مباشرة:
  - `/dashboard`
  - `/admin-dashboard`
  - `/instructor-dashboard`
  - `/supervisor-dashboard`
  - `/parent-dashboard`
  - `/reports`
  - `/profile`

القبول:

- الزائر لا يرى بيانات خاصة.
- الزائر لا يرى إدارة أو بيانات مستخدمين أو مدفوعات.
- المسارات المحمية تعيد لتسجيل الدخول أو تمنع الوصول بوضوح.

## المرحلة 4: رحلة المدير

يتم اختبار لوحة المدير بصريا:

- إدارة المستخدمين.
- إدارة المسارات والمواد والباقات داخل ساحة التعلم.
- إدارة عضويات المنصة العامة إن كانت موجودة ككيان منفصل.
- إدارة المدفوعات والطلبات.
- قبول/رفض دفع يدوي.
- الإعدادات المالية.
- المدارس والمجموعات والمشرفين.
- الدروس والاختبارات وبنك الأسئلة.
- التقارير.
- الإشعارات.
- النسخ الاحتياطي والعمليات.
- إعدادات التكاملات.

القبول:

- كل تبويب يفتح ولا يعلق.
- الجداول لا تنهار عند القوائم الفارغة.
- الأزرار المهمة تعمل أو تظهر رسالة خطأ مفهومة.
- أي تعديل يتم حفظه ويظهر بعد refresh.

## المرحلة 5: رحلة الطالب

يتم اختبار:

- تسجيل الدخول.
- لوحة الطالب.
- المسارات والدورات.
- فتح درس.
- فتح اختبار.
- إرسال اختبار ومراجعة النتيجة.
- المحتوى المقفل والمدفوع.
- السلة.
- إنشاء طلب دفع يدوي.
- متابعة طلباتي.
- الملف الشخصي.
- تسجيل الخروج.

القبول:

- الطالب لا يرى لوحة الإدارة.
- الطالب لا يغير حالة الدفع.
- الطالب لا يصل لمحتوى مدفوع قبل الاعتماد.
- الأخطاء تظهر برسالة مفهومة ولا تكسر الصفحة.

## المرحلة 6: رحلة المعلم

يتم اختبار:

- لوحة المعلم.
- المحتوى المخصص له.
- إنشاء/تعديل درس أو اختبار إذا كانت الصلاحية متاحة.
- نتائج الطلاب ضمن النطاق.
- منع الوصول لإدارة المستخدمين والمدفوعات العامة.

القبول:

- المعلم لا يرى بيانات خارج نطاقه.
- أي صلاحية إدارة عامة تكون ممنوعة من الواجهة ومن API.

## المرحلة 7: رحلة المشرف

يتم اختبار:

- لوحة المشرف.
- تقارير المدرسة أو المجموعة.
- الطلاب المرتبطون بالمشرف.
- منع وصول المشرف لمسارات المدير العامة.

القبول:

- المشرف يرى النطاق المسموح فقط.
- لا تظهر بيانات مدارس أو مجموعات غير مرتبطة.

## المرحلة 8: رحلة ولي الأمر

يتم اختبار:

- لوحة ولي الأمر.
- بيانات الأبناء المرتبطين.
- التقارير والتقدم.
- منع فتح إدارة أو بيانات طلاب آخرين.

القبول:

- ولي الأمر يرى بيانات الأبناء فقط.
- لا يستطيع تعديل بيانات الدفع أو المحتوى.

## المرحلة 9: فحص العضويات والباقات

يتم اعتماد التفريق التالي:

- صفحة `/pricing` تمثل عضويات المنصة العامة.
- باقات ساحة التعلم تدار داخل المسارات وساحة التعلم.

المطلوب فحصه:

- هل توجد إدارة واضحة لعضويات المنصة العامة؟
- هل أسماء الواجهة تقول "عضويات" وليس "باقات" عند الحديث عن `/pricing`؟
- هل باقات المسارات لا تختلط بعضويات المنصة؟
- هل الشراء أو الطلب المالي يربط كل نوع بالكيان الصحيح؟

إذا لم توجد إدارة عضويات عامة في لوحة المدير، تسجل كمشكلة Product/Admin Gap وليس Regression.

## المرحلة 10: فحص الحواف والأمان

يتم اختبار:

- تسجيل دخول خاطئ.
- حقول فارغة.
- نصوص طويلة.
- ضغط مزدوج على أزرار الحفظ.
- تحديث الصفحة أثناء التحميل.
- IDs غير موجودة في الروابط.
- محاولة فتح بيانات مستخدم آخر.
- محاولة تغيير حالة الدفع من الطالب.
- محاولة فتح API بإذن خاطئ.

## تصنيف الأخطاء

Critical:

- التطبيق لا يعمل.
- تسجيل الدخول مكسور.
- لوحة رئيسية مكسورة.
- تسريب صلاحيات أو بيانات.
- الدفع أو الوصول المدفوع غير آمن.
- البناء يفشل.

High:

- ميزة أساسية لا تعمل.
- نموذج رئيسي لا يحفظ.
- خطأ صلاحيات مهم.
- API يفشل في تدفق شائع.

Medium:

- حالة تحميل أو خطأ ناقصة.
- مشكلة استخدام تؤثر على تجربة المستخدم.
- تبويب يعمل جزئيا.

Low:

- تحذير غير كاسر.
- نص أو ترتيب بصري بسيط.

## صيغة توثيق كل Bug

Bug:
Location:
Role affected:
Steps to reproduce:
Expected behavior:
Actual behavior:
Root cause:
Fix applied:
Files changed:
Retest result:
Regression risk:

## بوابة الإغلاق

لا تغلق الدفعة إلا إذا تحقق الآتي:

- `npm run typecheck` PASS.
- `npm run build` PASS.
- `npm run server:check` PASS.
- `npm run server:build` PASS.
- `npm run smoke:health-readiness` PASS.
- `npm run smoke:frontend:strict` PASS مع commit match.
- `npm run smoke:operational` PASS.
- الفحص البصري للرحلات الأساسية تم فعلا.
- لا يوجد Critical أو High مفتوح.

## تقرير التسليم المطلوب

كل دفعة تغلق بتقرير يحتوي:

- `Deep Runtime Stability Report`.
- الرحلات المختبرة.
- الأدوار المختبرة.
- أخطاء console/network.
- Bugs المكتشفة والمصلحة.
- Bugs المؤجلة مع تصنيفها.
- نتائج أوامر التحقق.
- قرار جاهزية واضح:
  - `READY FOR DELIVERY`
  - أو `NOT READY - Critical/High remaining`
  - أو `OPERATIONALLY GREEN BUT VISUAL QA IN PROGRESS`

## حالة الفحص الأولية بتاريخ 2026-05-26

- `npm run smoke:health-readiness`: PASS.
- `npm run smoke:frontend:strict`: PASS.
- production commit match: `394b6050`.
- `/pricing` تعرض "عضويات المنصة" وتوضح أنها منفصلة عن باقات ساحة التعلم.
- تم رصد تحذيرات runtime في المتصفح الداخلي ويجب تتبعها في الدفعة البصرية التالية.

## المهمة الدقيقة التالية

ابدأ دفعة Visual QA:

1. حفظ دليل من جلسة المدير الحالية دون طباعة أسرار.
2. اختبار لوحة المدير تبويبا تبويبا.
3. تسجيل خروج ثم اختبار الطالب والمعلم والمشرف وولي الأمر بالحسابات المعتمدة.
4. تسجيل كل فشل بصيغة Bug.
5. إصلاح Critical/High فقط.
6. إعادة الفحص ونشر التغييرات عند وجود إصلاح.
