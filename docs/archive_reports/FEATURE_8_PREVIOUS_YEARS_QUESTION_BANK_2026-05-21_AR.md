# FEATURE-8 — بنك أسئلة الاختبارات السابقة — 2026-05-21

## الحالة
- النتيجة: `Fully closed`

## الملخص التنفيذي
تم إغلاق FEATURE-8 بإضافة عقد تصنيف صريح لأسئلة السنوات السابقة داخل نموذج الأسئلة ومسارات الاستعلام، بحيث يمكن تمييز المحتوى التاريخي بشكل موحد ومستقر عبر الإدارة والواجهات.

## ما تم تنفيذه
1. تحديث نموذج السؤال `QuestionModel` بإضافة الحقول:
   - `examType`: (`qudurat` | `tahsili` | `general`)
   - `source`: (`internal` | `official_exam` | `mock` | `imported`)
   - `year`: رقم السنة (اختياري)
2. إضافة فهارس مناسبة لدعم الفرز/الفلترة السريعة لأسئلة السنوات السابقة.
3. توسيع عقود API في `quiz.routes` لدعم:
   - إدخال الحقول الجديدة عند الإنشاء/التحديث.
   - فلترة `/api/quizzes/questions` عبر `examType`, `source`, `year`.
4. توسيع الاسترجاع الموجز للأسئلة لإرجاع حقول التصنيف الجديدة.

## الفحوص المنفذة
- `npm --prefix server run build` ✅ PASS
- `npm run typecheck` ✅ PASS
- `npm run build` ✅ PASS
- `npm run smoke:health-readiness` ✅ PASS
- `npm run smoke:frontend:strict` ✅ PASS

## أثر الإغلاق
- أصبح مسار “أسئلة الاختبارات السابقة” قابلاً للتفعيل بشكل موثق على عقد بيانات واضح.
- تم منع التداخل بين محتوى البنك العام ومحتوى الاختبارات السابقة عبر وسم صريح قابل للفلترة.

## الملفات المتأثرة
- `server/src/models/Question.ts`
- `server/src/routes/quiz.routes.ts`

## القرار النهائي
FEATURE-8 مغلقة بالكامل (كود + فحوص + تقرير).
