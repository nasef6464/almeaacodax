# نقطة مرجع كودكس الحالية

آخر تحديث: 2026-05-08

## آخر تثبيت

قاعدة مهمة للحساب التالي: التحكم في المجاني والمدفوع يكون حسب مكان العرض داخل إدارة مساحة التعلم، وليس كحكم عام على العنصر دائمًا. نفس الاختبار أو التدريب قد يكون مدفوعًا في مادة، ومفتوحًا داخل دورة أو باقة أخرى.

ملفات الدعم في موضوعات التأسيس أصبحت تربط صراحة بالموضوع عبر `libraryItemIds` مثل الدروس والتدريبات، حتى لا تظهر ملفات مكتبة في غير موضعها بسبب تشابه المهارات.

الدورات هي الدفعة التالية المقترحة: ربط منشئ الدورة ببنك الدروس والمسارات والمهارات الحقيقية، مع اختيار المعلم ونسبة الربح.

تم إصلاح صلاحيات السيرفر للباقات: تسليم التدريب المدفوع يراجع باقة التدريب، وتسليم الاختبار المدفوع يراجع باقة الاختبارات، والحارس `smoke:quiz-access` يحمي التفريق بينهما.

تم تدعيم حارس `smoke:reports-role` للتقارير العلاجية: خطة الطالب الأسبوعية، أزرار الشرح/التدريب/إعادة القياس، ملخص ولي الأمر، وخطة تدخل الأدوار الإدارية أصبحت عقودًا محروسة.

ابدأ من هنا عند فتح المشروع من أي حساب Codex جديد.

## ماذا تفعل أولًا

1. اقرأ هذا الملف.
2. اقرأ `docs/AGENT_HANDOFF_AR.md`.
3. اقرأ `docs/USER_NOTES_BATCH_AR.md` إذا كانت هناك ملاحظات جديدة من المستخدم.
4. شغّل `git status --short --branch`.
5. لا ترجع أي تعديل قبل فهمه.

## حالة التشغيل المحلية

- الواجهة المحلية تعمل على: `http://localhost:3000`
- الـ API المحلي يعمل على: `http://127.0.0.1:4000/api`
- فحص الصحة المحلي أعطى `status=ok` و`database=connected`.
- تم فتح المتصفح الداخلي على الصفحة الرئيسية بنجاح.
- عنوان الصفحة في المتصفح الداخلي: `منصة المئة | قدرات وتحصيلي`.
- الصفحة الرئيسية تظهر وليست شاشة بيضاء.

ملاحظة مهمة:

- عند فتح `http://127.0.0.1:3000` ظهرت تحذيرات API بسبب اختلاف origin/CORS.
- استخدم `http://localhost:3000` للفحص المرئي المحلي لأنه يطابق إعداد `CLIENT_URL`.

## آخر فحوص ناجحة

- `npm run smoke:operational` نجح في 2026-05-08 بنتيجة 71/71 بعد إصلاح مرجع سؤال مكسور في اختبار ظاهر للطالب.
- `npm run smoke:student-journey` نجح بنتيجة 6/6.
- `npm run smoke:learning-quiz` نجح بنتيجة 7/7.
- `npm run smoke:quiz-access` نجح بنتيجة 15/15.
- `npm --prefix server run build`
- `npm run typecheck`
- `npm run build`
- `npm --prefix server run check`
- `npm --prefix server run build`
- `npm run smoke:quiz-access`
- `npm run smoke:mock-exams`
- `npm run smoke:my-quizzes`
- `npm run smoke:reports-role`
- `npm run smoke:saher-skills`
- `npm run smoke:library-support`
- `npm run smoke:results`

## فحص رحلة التعلم

كان `npm run smoke:learning-quiz` يفشل بسبب بيانات إنتاج فيها مرجع سؤال مكسور:

- السؤال المفقود: `q_1777887544584_copy`
- موجود كمرجع داخل اختبارات منسوخة قديمة، لكنه غير موجود في `/api/quizzes/questions`.

الاختبارات المتأثرة:

- `quiz_1777887903014_copy`
- `quiz_1777887902510_copy`
- `quiz_1777887901798_copy`
- `quiz_1777887901198_copy`

تم الإصلاح في 2026-05-07:

- تم حفظ نسخة احتياطية محلية داخل `backups/quiz-ref-repair-20260507-135818.json`.
- تم حذف المرجع المكسور فقط من `questionIds` في الاختبارات الأربعة.
- بعد الإصلاح نجح `npm run smoke:learning-quiz` بنتيجة 7/7.

لا تعيد فتح هذا الجزء إلا إذا فشل الحارس مرة أخرى.

تم إصلاح مرجع مكسور إضافي في 2026-05-08:

- الاختبار المتأثر: `quiz_1777887887174`
- السؤال المفقود: `q_1777887544584_copy`
- تم حفظ نسخة احتياطية محدودة للاختبار قبل التعديل داخل `backups/quiz-ref-repair-2026-05-07T21-30-10-900Z-quiz_1777887887174.json`.
- تم حذف المرجع المكسور فقط من `questionIds`، فانخفض عدد المراجع من 6 إلى 5.
- بعد الإصلاح نجح `npm run smoke:operational` بنتيجة 71/71.

## الملفات المفتوحة الآن

- لا توجد تعديلات كود مفتوحة بعد commit `8d06331 Gate subject learning content by packages`.
- تمت إضافة حارس `scripts/smoke-student-learning-journey.mjs` وربطه بأمر `npm run smoke:student-journey`.

## نهج التطوير

- نطور دفعات صغيرة.
- كل دفعة لها حارس smoke أو فحص واضح.
- لا نعيد فتح جزء مغلق إلا عند فشل حارسه أو ظهور خلل واضح.
- واجهة الطالب وولي الأمر تبقى بسيطة.
- التفاصيل الكثيفة تبقى في الإدارة.
- لا نكتب أسرار أو مفاتيح داخل `docs` أو أي ملف مرفوع.
- فحص الأدوار الأساسي يتم عبر `npm run smoke:operational`، والمتصفح الداخلي يستخدم للتأكد المرئي المحلي عندما يكون محاكي الأدوار ظاهرًا.
