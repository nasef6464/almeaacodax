# ALMEAA — ALM-PRD-001 / Issues 234–237 Execution Checkpoint

**Date:** 2026-09-25  
**Scope:** `#234 → #235 → #236 → #237`  
**Production baseline at inspection:** `main@c378525cb505f4bfb548153c115991a2a18438b2`  
**Status:** **IN PROGRESS — NOT PRODUCTION CERTIFIED**

> هذا الملف هو المرجع الحالي لإغلاق آخر عوائق الإنتاج. لا يجوز اعتبار وجود الكود أو الـworkflow وحده دليلًا على نجاح تشغيل حي.

## 1) #234 — ALM-OPS-002 Runtime integrations

### Live release identity

Verified through the public production transport `https://almeaacodax.vercel.app/api`:

- Vercel Production is READY on `c378525cb505...`.
- `GET /health/live` → HTTP 200 and reports commit `c378525cb505`.
- `GET /health/ready` → HTTP 200, MongoDB connected, `ready=true`.
- `GET /health/scale-ready` → HTTP 503, `scaleReady=false`.
- Current scale blocker is exactly:
  `redis_not_configured_for_multi_instance_scale`.

This proves the frontend/backend release identity is synchronized, while Redis scale coordination is not yet configured.

### Integration configuration evidence

Read-only production database inspection, returning readiness booleans only and no secret values, found:

- Google provider is marked enabled but no client ID, client secret or callback URL is stored.
- Sentry provider is disabled and no configured secret was evidenced.
- Redis provider is disabled and no endpoint was evidenced in platform integration settings.

The runtime environment still requires the actual production values for:

- `REDIS_URL`;
- `SENTRY_DSN`;
- R2 account/bucket/access credentials before `R2_UPLOAD_ENABLED=true`;
- Google OAuth client ID/client secret/redirect URI.

### Render safety blocker

The connected Render account exposes exactly one workspace:

- `My Workspace`
- workspace id `tea-d86902uk1jcs73feaog0`

Render tooling requires explicit human confirmation of the workspace before service/environment mutations. No workspace was guessed or selected automatically.

**#234 close condition remains open** until Redis, Sentry, R2 upload, Google OAuth and Post Deploy live proof are complete.

## 2) #235 — ALM-DR-002 Real scheduled disaster recovery

### Existing verified tooling

The repository already contains checksum-enforced MongoDB and R2 backup/restore scripts.

### New fail-closed scheduler

This closure branch adds:

`.github/workflows/production-dr-backup.yml`

The workflow:

- runs daily at `01:17 UTC` / `04:17 Asia/Riyadh`;
- also supports manual dispatch;
- refuses to run successfully when any required backup/off-site secret is absent;
- creates verified MongoDB and R2 backups;
- requires an independent S3-compatible off-site destination;
- refuses source R2 bucket == off-site destination;
- verifies remote artifact presence;
- writes a persistent failure alert to Issue #235 with the failed Actions run link;
- intentionally does **not** upload production backup data as GitHub Actions artifacts.

A scheduled workflow in Git is not itself live backup evidence. First successful execution, off-site retention, alerting and restore drill are still required.

### Frankfurt recovery environment created

A separate FREE Atlas recovery project/cluster now exists:

- project: `almeaacodax-eu-recovery`;
- cluster: `almeaa-eu-recovery`;
- provider: AWS;
- region: `EU_CENTRAL_1` (Frankfurt);
- MongoDB: 8.0.32;
- state at verification: IDLE.

This is an isolated recovery/staging failure domain and is **not production**.

A bounded logical recovery exercise proved that records can be restored into this cluster while preserving identifiers. Verified examples include:

- `levels`: 2 / 2;
- `studyplans`: 6 / 6;
- `platformintegrationsettings`: 1 / 1;
- `homepagesettings`: 1 / 1;
- `backupactivities`: 4 / 4;
- `subjects`: 12 / 12;
- `classroomtemplates`: 1 / 1;
- `lessons`: 51 / 51.

Large documents exceeded the connector payload ceiling during this bounded exercise. Therefore this is **not** a substitute for the required full `mongodump → mongorestore` drill.

**#235 remains open** until a successful scheduled full MongoDB + R2 backup reaches independent off-site storage and full isolated restore drills produce measured RPO/RTO evidence.

## 3) #236 — ALM-PERF-002 Co-location and scale certification

### Current production topology

- Render API: Frankfurt.
- Production MongoDB Atlas: FREE / AWS `AP_SOUTHEAST_1` (Singapore).
- Recovery/staging Atlas: FREE / AWS `EU_CENTRAL_1` (Frankfurt).

No production `MONGODB_URI` cutover has occurred.

Read-only Atlas execution statistics on current production show representative direct database reads completing at approximately 0 ms execution time for the sampled small reads, reinforcing that multi-second application-route latency is not explained by raw query execution alone.

### Measurement hardening in this branch

- `scripts/smoke-production-speed.mjs` no longer defaults to retired Render hostname `almeaacodax-k2ux.onrender.com`; it uses the canonical same-origin `${FRONTEND_URL}/api`.
- New `scripts/measure-production-latency.mjs` records bounded read-only:
  - p50;
  - p95;
  - p99;
  - error rate;
  - response bytes;
  - status sets;
  - Render-origin evidence.

The script explicitly states that this evidence alone does not certify 500/1000 concurrent-user capacity.

**#236 remains open** until Redis is linked, API+Mongo network topology is co-located or otherwise materially improved, and staged current-release load evidence is captured with production-like telemetry.

## 4) #237 — ALM-GOV-001 Repository and network governance

### GitHub

Fresh branch evidence on `main@c378525...`:

- `protected=false`;
- branch protection `enabled=false`;
- required status check enforcement `off`;
- no required contexts/checks;
- repository rulesets endpoint returns `[]`.

The connected GitHub integration can read this state but does not expose branch-protection/ruleset mutation privileges. The direct protection endpoint returns HTTP 403 for the integration.

### Atlas network access

Fresh access-list evidence:

- `74.220.48.0/24` — Render outbound IP 1;
- `74.220.56.0/24` — Render outbound IP 2;
- `0.0.0.0/0` — broad public allow;
- one historical `/32` entry.

The Atlas connector available in this execution can inspect/create allow-list entries but does not expose removal. The broad rule must not be removed until current Render outbound ranges are re-confirmed and rollback/smoke are ready.

**#237 remains open** until repository-admin protection is enabled and the broad Atlas rule is safely removed with live connectivity proof.

## 5) Current execution order

1. Merge repository-side closure hardening only after exact-head CI is green.
2. Obtain explicit Render workspace confirmation, then inspect/link Redis and re-run `scale-ready`.
3. Restore Sentry/R2/Google production secrets from authoritative owner/provider sources only; never invent or copy stale values.
4. Configure independent DR destination secrets and require one successful scheduled backup.
5. Run full MongoDB and R2 isolated restore drills against recovery resources and record duration/age.
6. Decide and validate the production co-location cutover using the Frankfurt recovery/staging evidence.
7. Enable GitHub `main` protection/ruleset from a repository-admin surface.
8. Confirm current Render outbound CIDRs, remove Atlas `0.0.0.0/0` through an authorized Atlas admin surface, then run production smoke.
9. Produce Final Batch 15 PASS/FAIL evidence before any `PRODUCTION READY` claim.

## 6) Safety boundary

No production database was deleted, replaced or repointed during this work.  
No paid Atlas upgrade was created.  
No secret value was committed or printed.  
No broad Atlas access rule was removed without verified rollback.  
No CI gate was weakened.
