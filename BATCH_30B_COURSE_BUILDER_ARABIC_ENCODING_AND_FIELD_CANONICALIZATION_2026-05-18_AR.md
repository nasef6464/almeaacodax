# تقرير الدفعة 30B — توحيد إعدادات الدورات وإصلاح النص العربي
**التاريخ:** 2026-05-18
**الحالة:** Programmatically closed, production verification pending

## ما تم
- إزالة الازدواج الوظيفي لحقول `المسار/المادة` داخل `AdvancedCourseBuilder` والإبقاء على مسار إدخال واحد واضح.
- تثبيت ربط الحقول بحيث تغيير المسار أو المادة يعيد ضبط المهارات بشكل متسق.
- تنظيف النصوص العربية المشوّهة داخل بانيي الدورات (`AdvancedCourseBuilder` و`CourseBuilder`) لإيقاف ظهور `????`/رموز غير مفهومة في الواجهة.
- توثيق فحص الترابط كمرجع تنفيذي: `COURSE_LINKAGE_AUDIT_2026-05-18_AR.md`.

## الملفات المعدلة في هذه الدفعة
| الملف | نوع التغيير | السبب |
|---|---|---|
| `dashboards/admin/AdvancedCourseBuilder.tsx` | تعديل سلوكي + تنظيف نص | إزالة تكرار حقول المسار/المادة وتوحيد source-of-truth |
| `dashboards/admin/CourseBuilder.tsx` | تنظيف نص | معالجة ترميز عربي مشوّه |
| `PROJECT_STATUS.md` | تحديث حالة | إضافة إدخال رسمي لنتيجة فحص ترابط الدورات |
| `docs/NEXT_SESSION_HANDOVER_AR.md` | تحديث تسليم | إضافة نتائج الفحص وخطوات 30B/30C/30D |
| `COURSE_LINKAGE_AUDIT_2026-05-18_AR.md` | ملف تدقيق | توثيق المشكلات المترابطة وخطة الإغلاق المرحلية |

## الملفات التي كانت معدلة مسبقًا ولم يتم لمسها
- توجد ملفات كثيرة معدلة مسبقًا خارج نطاق هذه الدفعة (auth/payments/rbac/monitoring وغيرها) وتم تركها كما هي بدون تعديل.

## نتائج الفحوص
| الأمر | النتيجة | ملاحظات |
|---|---|---|
| `npm --prefix server run build` | ✅ PASS | بدون أخطاء |
| `npm run typecheck` | ✅ PASS | المحاولة الأولى timeout ثم إعادة التشغيل PASS |
| `npm run build` | ✅ PASS | Vite build ناجح |
| `npm run smoke:health-readiness` | ✅ PASS | readiness contract ناجح |

## فحص الإنتاج
- لم يتم تنفيذ فحص بصري إنتاجي نهائي لهذه الدفعة من داخل الحساب الحالي (يتطلب تأكيد deploy على Vercel/Render بعد push).
- الحالة تبقى: **Programmatically closed, production verification pending**.

## خطوات التحقق اليدوي
1. افتح لوحة الإدارة > إدارة الدورات > تعديل/إنشاء دورة.
2. تحقق أن حقول `المسار/المادة` تظهر مرة واحدة فقط بدون تكرار.
3. غيّر المسار وتأكد أن المواد المعروضة تتفلتر حسب المسار.
4. غيّر المادة وتأكد أن المهارات تعاد تهيئتها ولا تحمل بقايا مادة قديمة.
5. افتح باني المناهج وتأكد أن النصوص العربية تظهر بشكل صحيح بدون `????`.
6. افتح صفحة تعلم الطالب لنفس المسار/المادة وتحقق أن ربط الدورة لا يتأثر.

## المخاطر المتبقية
- يلزم إغلاق `BATCH 30C` لعقد ظهور الدورة (Admin -> Student) وتثبيت empty-state واضح.
- يلزم إغلاق `BATCH 30D` لحارس نطاق الاستدعاء (دروس/اختبارات) حسب المسار/المادة.
- التحقق الإنتاجي النهائي يحتاج نشر commit ثم اختبار حي على رابط Vercel.

## الدفعة التالية المقترحة
- **BATCH 30C — Course Visibility Contract (Admin -> Student)**
