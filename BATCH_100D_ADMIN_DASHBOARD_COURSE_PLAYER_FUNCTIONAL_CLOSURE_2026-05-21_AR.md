# BATCH 100D — فحص وظائف لوحة الإدارة وإعدادات الصفحة ومشغل الدورة

**التاريخ:** 2026-05-21  
**الحالة:** Programmatically closed, production data follow-up required  
**النطاق:** إصلاح كاش إعدادات الصفحة الرئيسية + إثبات عقود منشئ الدورات/استدعاء الدروس والاختبارات/مشغل الدورة + فحص إنتاج حي.

## السبب
- المالك أبلغ أن صورة البطل في إعدادات الصفحة الرئيسية لا تظهر بعد تعديلها.
- المالك أبلغ أن إعدادات الدورات تحتاج ربطًا واضحًا بين المسار/المادة/المهارات، وأن استدعاء الدروس والاختبارات يحتاج بحث/فلترة.
- المالك طلب إضافة فحص مشغل الدورة وكل جزء فيه ضمن قاعدة الإغلاق.

## ما تم تنفيذه
- تمت إضافة تفريغ كاش `homepage-settings` بعد حفظ إعدادات الصفحة الرئيسية حتى لا تبقى الصفحة العامة تعرض صورة قديمة من `sessionStorage`.
- تم تحديث الرد الجديد داخل الكاش العام بعد الحفظ لضمان اتساق العرض بعد التعديل.
- تمت إضافة smoke contract جديد يثبت:
  - وجود تفريغ كاش إعدادات الصفحة الرئيسية.
  - وجود فلترة استدعاء الدروس حسب المسار والمادة.
  - وجود فلترة استدعاء الاختبارات حسب المسار والمادة.
  - وجود البحث داخل استدعاء الدروس والاختبارات.
  - أن نافذة الاستدعاء لم تعد محدودة جدًا وتستخدم ارتفاعًا مرنًا.
  - أن إعدادات الدورة تحتوي ربط المادة وتصفير المهارات عند تغيير النطاق.
  - أن مشغل الدورة يدعم الفيديو/الاختبار/الملف/حالة عدم وجود دروس.
  - أن صفحة التعلم تستخدم ربط المسار والمادة عند عرض الدورات.

## الملفات المعدلة في هذه الدفعة فقط
| الملف | نوع التغيير | السبب |
|---|---|---|
| `services/api.ts` | تعديل وظيفي صغير | تفريغ كاش إعدادات الصفحة الرئيسية بعد الحفظ |
| `scripts/smoke-batch100d-admin-course-flow.mjs` | ملف جديد | إثبات عقود 100D للدورات والصفحة الرئيسية ومشغل الدورة |
| `package.json` | إضافة script | تشغيل smoke الجديد باسم واضح |
| `BATCH_100D_ADMIN_DASHBOARD_COURSE_PLAYER_FUNCTIONAL_CLOSURE_2026-05-21_AR.md` | تقرير جديد | توثيق الإغلاق والفحص |
| `PROJECT_STATUS.md` | تحديث حالة | تسجيل حالة الدفعة الحالية |
| `docs/SPARK_BATCH_LEDGER_AR.md` | تحديث ledger | تسجيل الدفعة والنتيجة |
| `docs/NEXT_SESSION_HANDOVER_AR.md` | تحديث التسليم | تمكين الحساب التالي من المتابعة بنفس القاعدة |

## ملفات كانت معدلة مسبقًا ولم يتم لمسها
- توجد تعديلات قديمة كثيرة خارج هذه الدفعة في ملفات مثل `App.tsx`, `contexts/AuthContext.tsx`, `pages/QuizPage.tsx`, وملفات dashboards/server متعددة.
- لم يتم عمل revert أو staging لهذه الملفات.
- لم يتم استخدام `git add .`.

## الفحوص
| الأمر | النتيجة | ملاحظات |
|---|---|---|
| `npm run smoke:batch100d-admin-course-flow` قبل الإصلاح | FAIL متوقع | فشل بسبب غياب `clearPublicCache` |
| `npm run smoke:batch100d-admin-course-flow` بعد الإصلاح | PASS | أثبت عقود 100D |
| `npm --prefix server run build` | PASS | TypeScript server build ناجح |
| `npm run typecheck` | PASS | TypeScript frontend ناجح |
| `npm run build` | PASS | Vite build ناجح |
| `npm run smoke:course-builder` | PASS | 4 checks ناجحة |
| `npm run smoke:course-visibility` | PASS | 3 checks ناجحة |
| `npm run smoke:homepage-hero` | PASS | عقد صورة الصفحة الرئيسية ناجح |

## فحص الإنتاج الحي
- تم فحص صفحة التعلم إنتاجيًا على:
  `https://almeaacodax.vercel.app/category/p_1777779639431?subject=sub_1777779748206&tab=courses`
- النتيجة: الصفحة تعرض دورات `حمكشة` و`ب ال` ولا تعرض دورة `التأسيسي الشامل كمي`.
- تم فحص رابط مشغل الدورة إنتاجيًا:
  `https://almeaacodax.vercel.app/course/course_current_p_1777779639431_sub_1777779748206_foundation`
- النتيجة: الصفحة تعرض `الدورة غير متاحة حاليًا`.
- تم فحص API العام:
  - `/api/courses?limit=200` يرجع 5 دورات فقط ولا يحتوي على `course_current_p_1777779639431_sub_1777779748206_foundation`.
  - `/api/courses/course_current_p_1777779639431_sub_1777779748206_foundation` يرجع `404 Course not found`.
- الاستنتاج: مشكلة الدورة المحددة في الإنتاج هي مشكلة بيانات/نشر/معرّف دورة غير ظاهر من السيرفر، وليست كسرًا مباشرًا في مشغل الدورة.

## لقطات الفحص المحلي غير المرفوعة
- `audit-artifacts/batch100d/learning-courses-production.png`
- `audit-artifacts/batch100d/course-unavailable-production.png`

## المخاطر المتبقية
- لا يمكن اعتبار ظهور دورة `التأسيسي الشامل كمي` مغلقًا إنتاجيًا لأنها غير موجودة في قائمة API العامة الحالية أو غير مرجعة من السيرفر.
- يحتاج الأمر دفعة بيانات/نشر مستقلة لفحص: هل الدورة محفوظة في MongoDB؟ هل `isPublished/showOnPlatform/approvalStatus/pathId/subjectId/id` صحيحة؟ وهل توجد نسخة محلية غير منشورة؟
- إدارة المجموعات والعلاقات بين المدارس/المشرفين/المعلمين/الطلاب/الأهالي ما زالت تحتاج دفعة فحص مستقلة كما طلب المالك.

## هل تم إغلاق الخطر الوظيفي؟
- إصلاح كاش إعدادات الصفحة الرئيسية: نعم برمجيًا.
- عقود منشئ الدورات ومشغل الدورة: نعم برمجيًا.
- ظهور الدورة المحددة في الإنتاج: لا، يحتاج فحص بيانات إنتاجية/نشر مستقل.

## خطوات التحقق اليدوي
1. افتح لوحة الإدارة كمدير.
2. افتح إدارة الصفحة الرئيسية وعدّل صورة البطل ثم احفظ.
3. افتح الصفحة الرئيسية في نفس المتصفح أو بعد refresh وتأكد أن الصورة الجديدة تظهر بدون انتظار دقيقتين.
4. افتح إعدادات دورة وتأكد من وجود المسار والمادة والمهارات.
5. غيّر المسار أو المادة وتأكد أن المهارات تتصفر عند تغير النطاق.
6. افتح بناء المنهج واستدعاء درس/اختبار، واستخدم البحث وفلترة المسار/المادة.
7. افتح صفحة التعلم للمادة وتأكد من ظهور الدورات المنشورة فعليًا.
8. افتح دورة موجودة من نفس الصفحة وتأكد من مشغل الفيديو/الاختبار/الملف.

## الدفعة التالية المقترحة
`BATCH 100E — Production Course Data Visibility Repair + Groups/Relationships Audit Entry`

## إغلاق النشر بعد GitHub/Vercel
- Commit المرفوع: 755a96.
- GitHub push: تم إلى origin/main.
- Vercel production: 
pm run smoke:frontend:strict PASS في المحاولة الثانية ويخدم commit 755a96.
- Render health: 
pm run smoke:health-readiness PASS، و/api/health يرجع status=ok وeady=true. لم تتغير خدمة backend وظيفيًا في هذه الدفعة.
- فحص الدورة الإنتاجية بعد النشر: ما زالت course_current_p_1777779639431_sub_1777779748206_foundation ترجع 404، وهذا مؤكد كمتابعة بيانات/نشر في BATCH 100E.
- وقت التحديث: 2026-05-21 14:58:47.
