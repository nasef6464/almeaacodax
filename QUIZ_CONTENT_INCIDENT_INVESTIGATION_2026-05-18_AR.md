# QUIZ_CONTENT_INCIDENT_INVESTIGATION_2026-05-18_AR

## التاريخ
2026-05-18

## الحالة
تم التحقق الحي من السبب الجذري (تحليل + إثبات API Production)

## المشكلة التي ظهرت للمستخدم
- عند بدء بعض الاختبارات تظهر رسالة: "لا توجد أسئلة متاحة".
- داخل بعض الأسئلة في الإدارة/التجربة، الصور غير ظاهرة.
- يوجد إحساس ببطء في الموقع.

## ماذا تم فحصه
- Production API:
  - `GET /api/health` -> خدمة سليمة (database/redis ready).
  - `GET /api/quizzes?pathId=p_1777779653351&subject=sub_1777784609152...` -> يعرض اختبارات منشورة.
  - `GET /api/quizzes/questions?ids=q_smoke_math_learning_1,q_smoke_math_learning_2,q_smoke_math_learning_3...` -> يرجع `[]`.
  - `GET /api/quizzes/questions?ids=q_1777887540184_copy,...` -> بعض السجلات ترجع مع `imageUrl` فارغ.
- مراجعة كود السيرفر:
  - `server/src/routes/quiz.routes.ts` يعيد `Quiz has no valid questions` عندما لا يمكن resolve لأي سؤال صالح من `questionIds`.
  - الواجهة تعرض الترجمة العربية لهذه الحالة: "لا توجد أسئلة متاحة".

## السبب الجذري (مؤكد)
1. اختبارات منشورة تشير إلى `questionIds` غير موجودة فعليًا في قاعدة البيانات أو غير قابلة للـresolve.
2. جزء من الأسئلة المرجعية موجود لكنه ببيانات محتوى ناقصة (`imageUrl` فارغ، وأحيانًا `text` فارغ).
3. هذا خلل سلامة بيانات publishing/content-linking وليس خلل تصميم UI.

## سبب بطء الموقع (الحالة الحالية)
- الصحة العامة للخدمة جيدة (`/api/health` سليم).
- البطء يظهر غالبًا في مسارات bootstrap الثقيلة، وليس في minimal bootstrap.
- يلزم استكمال تجزئة learning bootstrap (المسار الثقيل) لتثبيت الأداء تحت حمل أعلى.

## فحص حي (Visual/API)
- فحص API حي تم بنجاح مع أدلة واضحة على mismatch بين quiz->questionIds.
- الفحص البصري داخل المتصفح المدمج يعتمد على جلسة المستخدم، وقد تم ربط الأعراض بنتيجة API مباشرة.

## تحديث تحقق حي لاحق (Production) — 2026-05-18
- تم نشر إصلاحات الحماية (`3490e1e`) على الإنتاج (commit ظاهر في `/api/health`).
- نتيجة `GET /api/quizzes` للمتعلم:
  - لم تعد الاختبارات ذات `questionIds` المكسورة تظهر في القائمة.
  - انخفض إجمالي القائمة في العيّنة من 10 إلى 7 (بعد استبعاد الاختبارات غير الصالحة للمتعلم).
- ملاحظة: هذا يمنع ظهور الخطأ للطالب، لكنه لا يغني عن إصلاح البيانات الأصلية من الإدارة.

## الإجراء المقترح في الخطة
- تنفيذ دفعة مخصصة:
  - **BATCH 20ZG — Quiz Content Integrity & Media Availability Guard Rails**
- نطاقها:
  1. تدقيق شامل لكل Quiz منشور: كل `questionIds` يجب أن تكون قابلة للـresolve.
  2. منع نشر أي Quiz جديد إذا لم يملك أسئلة صالحة مرئية للمتعلم.
  3. إضافة تقرير إداري يحدد الروابط المكسورة (quizId/questionId) قبل الإتاحة.
  4. تدقيق جودة المحتوى (text/imageUrl/options) دون تغيير تصميم الواجهة.

## ملاحظة
- لم يتم تعديل UI أو الألوان أو التخطيط.
- هذا الملف تشخيصي لتحديث الخطة قبل بدء دفعة الإصلاح.
