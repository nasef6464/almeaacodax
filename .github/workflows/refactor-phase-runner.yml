name: Temporary Refactor Phase Runner

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  run-schools-relations-import-phase:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - name: Checkout target refactor branch
        uses: actions/checkout@v4
        with:
          ref: refactor/repository-v2-safe
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            server/package-lock.json

      - name: Apply relations import presentation boundary
        run: python tools/refactor/apply-schools-relations-import-boundary.py

      - name: Install frontend dependencies
        run: npm ci

      - name: Install API dependencies
        run: npm --prefix server ci

      - name: Direct relations import boundary contract
        run: node scripts/smoke-schools-relations-import-boundary-contract.mjs

      - name: Fast targeted schools gate
        run: node tools/refactor/quick-check.mjs schools

      - name: Full schools workspace phase review
        run: node tools/refactor/phase-review-schools-workspace.mjs

      - name: Restore generated audit snapshots
        run: git checkout -- docs/architecture/generated

      - name: Finalize verified phase
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          checkpoint_path = Path('docs/architecture/REFACTOR_V2_LATEST_CHECKPOINT_AR.md')
          checkpoint = checkpoint_path.read_text(encoding='utf-8')
          checkpoint = checkpoint.replace(
              '## Relations Import presentation boundary — قيد التحقق',
              '## Relations Import presentation boundary — Full Phase Review PASS',
              1,
          )
          if 'Relations Import Full Phase Review: **PASS**' not in checkpoint:
              checkpoint += '\n- Relations Import Full Phase Review: **PASS** قبل إنشاء commit الدفعة؛ القبول النهائي ينتظر Standard Safety Gate.\n'
          checkpoint_path.write_text(checkpoint, encoding='utf-8')
          PY

          git rm -f tools/refactor/apply-schools-relations-import-boundary.py
          git config user.name "almeaa-refactor-bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git status --short
          git commit -m "refactor(schools): extract relations import presentation"
          git push origin HEAD:refactor/repository-v2-safe
